/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath: '/auracomplexnew', // Добавили эту строку!
};

export default nextConfig;